version: '3.8'

services:
  bagman:
    image: bagman
    ports:
      - "${DASHBOARD_PORT}:${DASHBOARD_PORT}"
    volumes:
      - ${RECORDING_PATH}:${RECORDING_PATH}
    environment:
      - DASHBOARD_PORT=${DASHBOARD_PORT}
